extends layout

block content
  h1= title
  p I am missing my sister.
  button(type="button" class="btn btn-secondary" onclick="boost()") Sample Button
  div#boost Add in extra
  script.
    let value = 0;
    let amt = 3;

    function boost() {
      amt += 1;
      value += amt;
      document.getElementById("boost").textContent = `Boosted ${value} by ${amt}`;
    }
